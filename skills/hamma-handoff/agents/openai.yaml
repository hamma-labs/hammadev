interface:
  display_name: "Hamma Handoff"
  short_description: "Attach repository memory across coding agents"
  default_prompt: "Use $hamma-handoff to attach this project's memory from Claude Code."

interface:
  display_name: "Hamma Resume"
  short_description: "Attach repository memory in a fresh chat"
  default_prompt: "Use $hamma-resume to load this project's memory and honor its execution mode."

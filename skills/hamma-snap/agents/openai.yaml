interface:
  display_name: "Hamma Snap"
  short_description: "Persist this session into repository memory"
  default_prompt: "Use $hamma-snap to save this session's durable knowledge and task epoch."
